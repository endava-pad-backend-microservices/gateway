#cloud config for gateway
spring:
  cloud:
    config:
      uri:
      - http://${CONFIG_URL}:${CONFIG_PORT}/
      name: dev
      profile: dev
      label: configuration
  application:
    name: configuration 